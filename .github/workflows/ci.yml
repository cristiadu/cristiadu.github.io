name: Build, Lint, Test, and E2E Tests (CI)

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write
  pull-requests: write

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          cache: 'pnpm'

      - name: Install Dependencies
        run: pnpm install --frozen-lockfile

      - name: Install Playwright Browsers
        run: pnpm exec playwright install --with-deps

      - name: Lint
        run: pnpm run lint

      - name: Test with Coverage
        run: pnpm run test:coverage

      - name: Coverage Report
        uses: davelosert/vitest-coverage-report-action@v2

      - name: Build
        run: pnpm run build

      - name: E2E Tests
        id: e2e
        run: pnpm run test:e2e:update

      - name: Commit Linux Screenshots
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add src/tests/e2e/__screenshots__/linux/
          git commit -m "chore: add linux playwright screenshots" || echo "No changes"
          git push origin HEAD:${{ github.head_ref }}

      - name: Upload Playwright Report
        uses: actions/upload-artifact@v4
        if: always() && steps.e2e.outcome != 'skipped'
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30


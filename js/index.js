import PortfolioApp from './PortfolioApp';
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <React.StrictMode>
        <PortfolioApp />
    </React.StrictMode>
);

export default root;
